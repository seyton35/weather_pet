name: weather_repository
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  weather_api:
    path: "../weather_api"
  local_storage_tracking_locations_api:
    path: "../local_storage_tracking_locations_api"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
