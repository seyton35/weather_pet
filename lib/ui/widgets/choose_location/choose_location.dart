import 'package:flutter/material.dart';

class ChooseLocatrion extends StatelessWidget {
  const ChooseLocatrion({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('main screen'),
    );
  }
}
